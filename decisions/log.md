# Decision Log

Append-only. When a meaningful decision is made, log it here.

Format: [YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...

---

[2026-06-18] DECISION: Verified that the SOD report covers fresh invoice/payment EMAILS (the coverage flag from making Morning Coffee purely-personal) and chose NOT to harden it — accept the narrow edge case as-is (John's call). | REASONING: Checked today's live 10 AM run in `#ops-command`: the 9 AM Morning Triage post surfaced the invoice/payment emails (Airwallex USD 2,300 deposit + DBS transfer in FYI, Shareef in Needs-Reply), and the SOD report then pulled them into its BAU/Follow-ups — `Airwallex — USD 2,300 deposit received; confirm client match` is exactly the email Morning Coffee now drops, and it landed in SOD the same morning. Invoice blockers are independently carried from the EOD. So the split works in practice; no coverage lost. | CONTEXT: One known edge case accepted: SOD's email coverage rides on the (optional) Morning Triage post; SOD's own direct unread-Gmail invoice scan only fires in Step 1b when John's morning DUMP is missing — NOT when Morning Triage is missing. So if Morning Triage fails AND John posts a dump the same day, a brand-new overnight invoice email could be missed (Morning Coffee used to backstop this). Low probability (Morning Triage = n8n EuT6REDs5PUaoycE, daily/reliable). Optional future hardening (declined for now): make SOD's Gmail invoice scan run whenever Morning Triage is missing — note SOD is a remote claude.ai routine (prompt drift), so it'd need a RemoteTrigger edit + SKILL.md sync. No code/skill change made (decision only); this note committed 2026-06-18.

---

[2026-06-18] DECISION: Dropped `#ad-production-internal` (`C0AGEM919QV`) from Morning Coffee and removed Step 4 (the `slack` bot-token channel scan) entirely. | REASONING: After the payments channel was removed earlier today, `#ad-production-internal` was the only channel left in Step 4. The 2026-06-18 dry run showed it yields almost nothing personal for Noa (1 mention in 30 messages) while being the single most expensive pull (98K-char history that blew the tool token limit), and it is IM8-flavored — IM8 engagement has ended, so it should be out of triage/reporting anyway. With both channels gone, Step 4 had no purpose. | CONTEXT: Step 4 replaced with a tombstone documenting the removal and how to re-add a bot-only channel if one ever genuinely carries Noa's personal asks. Net effect: Morning Coffee no longer uses the `slack` bot-token MCP at all (DM read + send use `claude_ai_Slack` / John's OAuth; broad scan uses `slack-noa`). The `slack` bot MCP stays in `.mcp.json` — still used by SOD (channel history of `#ops-command`), ops-report, and krave-bot. Updated both skill copies (Step 4 tombstone, Key Data row, MCP Tools Reference row, scheduling dependency line) — committed in 2337b9d. No deploy (local Task Scheduler); this note committed 2026-06-18.

---

[2026-06-18] DECISION: Cost-trimmed Morning Coffee's broad Slack scan (Step 5) — excluded bot-fed/broadcast channels, capped the scan 20 → 10 channels (limit 15 → 10/channel), and added per-run yield instrumentation to converge on a proven allowlist. | REASONING: A compose-only dry run showed the 20-channel `slack-noa` sweep returned ZERO personal items in a 24h window — every candidate was a bot digest (`halo-home-shopify` Halo AI), a `-reporting` channel (bot-fed), or a stale thread (noaos/int-comrad last active ~late May) — while two channels' histories were large enough (55K / 98K chars) to exceed the tool's token limit and force expensive workarounds. The old Tier A even prioritized `-reporting` channels, which structurally never carry a human @Noa ask. So the sweep was nearly pure waste. Cutting the obvious structural noise is safe and deterministic; the instrumentation lets the candidate set be tightened further with real data instead of guesswork. | CONTEXT: New Step 5 logic: keep `is_member && !is_archived`; EXCLUDE names ending `-reporting`/`-adreporting`/`-updates`, plus `halo-home-shopify`, `general`/`random`/`emojis`/`inboundleads`/`automation-wishlist`/`freelanceleadspro-updates`, plus `num_members >= 25` (org-wide broadcast); rank survivors (Tier A = `krave-x-`/`int-`/`creator`/`noa*` + ≤4-member groups, Tier B by `updated`), take top 10, `get_channel_history` limit 10, skip channels whose 24h messages are all `bot_id` posts. Appends `{date,scanned,yielded}` to new local-only gitignored `.claude/skills/morning-coffee/scan-stats.jsonl`; refinement rule = after ~10 weekday runs, cut never-yielding channels into the exclusion list and log it here. Synced both skill copies (`.claude/` + `.agents/`), added Key Data rows + gitignore pattern. Step 4's `#ad-production-internal` (bot-token channel) left in place but flagged as a likely next cut — IM8-flavored (engagement ended) and only 1 Noa mention in 30 msgs in the same dry run. No deploy (local Task Scheduler skill); effective next run. NOT committed.

---

[2026-06-18] DECISION: Gave Morning Coffee (1) cross-day carry-over state and (2) a one-line "Top of mind" synthesis, and scoped it **purely personal** by deferring all ops/finance to the SOD report. | REASONING: Morning Coffee was stateless — it forgot every day, so it couldn't follow up on tasks Noa said she'd do or on emails she hadn't replied to, and a 3-day-overdue item read identically to a 1-hour-old one (SOD already has a carry-over contract; Morning Coffee had none). It was also a flat list of sections with no lead, working against the "scannable in 60 seconds" goal. Separately, Morning Coffee and SOD both fire at 10 AM PHT and both surfaced invoices/payments/overdue alerts, so Noa saw the same item twice; John chose to make Morning Coffee purely personal and let SOD own ops/finance. | CONTEXT: Carry-over persists in a new local-only gitignored file `.claude/skills/morning-coffee/state.json` (`open_tasks` + `open_replies`, each with `first_seen`): Step 0b loads it, Step 2 tags carried unreplied emails `⏳ since [Day]`, Step 5b reconciles tasks (done → drop / optional `✅ Closed`; still open → `↳ from [Day] — still open?`), Step 8 writes it back; missing/failed state never blocks the send. `☕ Top of mind:` is a one-sentence lead under the greeting, always present, composed last (priority: hard-deadline email > deep-work calendar conflict > carried item ≥2 days > dominant signal > warm light-day line). Purely-personal removed: the `#payments-invoices-updates` channel from the Slack scan, and invoice/payment/overdue/AR-AP emails from all buckets (incl. `_Payment_Received` receipts — this reverses the prior "keep payment receipts in FYI" note). Edited both skill copies (`.claude/` + `.agents/`), added the gitignore pattern. No SOP / n8n workflow exists for this skill (local Windows Task Scheduler), so no deploy needed — changes take effect on the next run. Coverage note to verify: SOD must reliably surface fresh invoice-approval *emails* now that Morning Coffee no longer does. NOT committed.

---

[2026-06-17] DECISION: Backed the krave-bot invoice guard with a deterministic CODE block (not just the system prompt) in `projects/krave-bot/tools/airwallex.js`. | REASONING: The bot is an LLM following prompt rules — that lowers but doesn't eliminate the risk of mis-billing. `createInvoice` now throws if `billing_customer_id` is a known test ID; `finalizeInvoice` GETs the invoice and blocks the same way. So the bot physically cannot create or finalize a real invoice against Krave Test / Krave Internal Test / Test Address Corp — matching the n8n code gate's guarantee. | CONTEXT: `TEST_CUSTOMER_IDS` = the 4 known IDs (bcus_sgpdgmqz9hic0zyo485, bcus_sgpdp7xdxhi30oyhmri, bcus_sgpdb6h5zhi2uty4o1o, bcus_sgpdn67v7hhopoh228m). Unit-tested locally (blocks test ID before any network call; lets a real ID through). Deployed via push to github master (Render krave-ai auto-deploy). A newly-created customer reusing john@kravemedia.co would not be ID-blocked, but the prompt rule + the n8n gate still block that email. NOT committed at time of this line — see push below.

---

[2026-06-17] DECISION: Added a test-customer hard block + ambiguity guard + duplicate guard to every client-invoice creation path after a Dojocare invoice was billed to the internal "Krave Test" customer 4× today (INV-GU3TGH7X-0001..0004, USD 4600, all VOIDED; billing email was john@kravemedia.co so nothing reached an external party). | REASONING: Root cause confirmed mechanically — the Airwallex `email` filter works (accounting@dojocare.co → exactly Dojocare), but `john@kravemedia.co` maps to FOUR internal test customers (Krave Test ×2, Krave Internal Test, Test Address Corp) and the resolver did `customers.find(email match)` → returned the first ("Krave Test"). The `name` query does NOT filter server-side (returns the full customer list), so any name-fallback that took first-of-list could also mis-resolve. No path had a duplicate guard, so a re-submit billed again each time = the 4×. Guard rule across all paths: (1) match by exact email within results, never first-of-list; (2) HARD BLOCK email john@kravemedia.co / names containing "test" / test IDs bcus_sgpdgmqz9hic0zyo485, bcus_sgpdp7xdxhi30oyhmri, bcus_sgpdb6h5zhi2uty4o1o, bcus_sgpdn67v7hhopoh228m; (3) >1 email match → stop (ambiguous); (4) same client+currency+amount draft/sent in last 3 days → stop. | CONTEXT: Edited (NOT yet deployed): `.claude/skills/client-invoice-creation/SKILL.md` (Step 4a rewrite + 4a.5 dup guard + Mode 2 pre-finalize guard), `.agents/skills/client-invoice-creation/SKILL.md`, `references/sops/client-invoice-creation.md`, `projects/krave-bot/system-prompt.js` (INVOICE RULES 5b/5c), `n8n-workflows/deploy-invoice-request-intake.js` (RESOLVE_CUSTOMER_CODE + new "Customer Safety Gate" IF node routing blocks to the DM-John fallback), `WORKFLOWS.md` (node flow + intake rule). Deploy still required by John (creds not in this env): `node deploy-invoice-request-intake.js` (n8n intake) and `git push github master` (krave-bot → Render auto-deploy). Skill/SOP take effect immediately (no deploy). The 4 test invoices were already voided + tracker rows deleted by John. NOT committed.

---

[2026-06-15] DECISION: Bumped Halo intel per-hashtag scrape cap 20 → 30 (TikTok `resultsPerPage`, IG `resultsLimit`); raised the Apify URL `&limit` 200 → 300 to match. | REASONING: Team wants a larger ranking pool. 8×30 = 240 results/platform/run (~$0.96 TikTok + ~$0.55 IG ≈ $1.51/run, up from ~$1.01). Raised the URL limit in lockstep because it must stay ≥ results/run or the scorer silently drops paid results. | CONTEXT: Deployed + verified live via GET (resultsPerPage:30, resultsLimit:30, both fetch URLs limit=300, active). Watch the first run's duration — the Apify nodes have timeout=300s; 30/hashtag should stay within it, but if a fetch times out `continueOnFail` returns empty for that platform. Still far under the $50 STARTER cap (~$6–8/cycle at weekly cadence). Not committed.

---

[2026-06-15] DECISION: Restructured the Halo intel report's trend synthesis into Shin's requested sectioned layout, and raised the Apify dataset `limit` from 50 to 200. | REASONING: Shin's feedback was that the 2-paragraph prose synthesis was hard to scan and should be organized into labeled sections. Changed the Claude output contract for `trendSynthesis` from a prose string to a structured object (`overview`, `tiktokHighlights`, `instagramHighlights`, `crossPlatformFormat`, `haloPriorities`[3]) and rewrote the Slack + email formatters to render emoji-headed sections, bullets, and numbered priorities. Separately — now that the scrape is correctly capped at 160 results/run — the old `&limit=50` on the Apify URL was discarding ~110 already-paid results before scoring (and skewing the scorer toward the first hashtags); raised to 200 so the scorer sees everything billed. No cost change (cost is set by `resultsPerPage`/`resultsLimit`, not the URL limit). | CONTEXT: Verified locally before deploy with a throwaway harness (compiled both Code nodes, ran Format Report against a mock structured analysis → correct Slack/email render; bad-JSON path degrades to "synthesis unavailable" without throwing), then redeployed + confirmed live via GET (limit=200 on both fetch nodes, Format Report carries the sectioned synthesis + synthHtml/synthText, Prepare Claude Request asks for the structured object, `resultsPerPage:20` intact, workflow active). New format appears on the next scheduled run (Mon 7AM PHT) — no manual run triggered (would email 5 recipients). Docs synced: WORKFLOWS.md (node flow + limit note), skill `.claude` + `.agents`. NOT committed — more Shin fixes pending.

---

[2026-06-15] DECISION: Fixed a ~5× Apify overspend on the Halo Weekly Intelligence Report (`5ZqTSaUEtxnAndiY`) — TikTok scrape body changed from `maxPostsPerPage: 20` to `resultsPerPage: 20`. | REASONING: Apify was at $41.05 of the $50 STARTER cap this cycle (May 22–Jun 21). ~All of it ($41.03) was `PAID_ACTORS_PER_EVENT` (pay-per-result rental actors), not compute/storage/proxy (those were cents). Two Halo-intel scrapers were ~100% of spend: `apify/instagram-hashtag-scraper` $24.68 — but 64 of its 69 runs were a single-day May-23 test burst (no Apify schedule exists), not a recurring drain — and `clockworks/tiktok-hashtag-scraper` $16.00 across 5 runs = $3.20/run. Root cause of the TikTok cost: the deploy body set `maxPostsPerPage: 20`, which is NOT a field in the actor's input schema (the real cap is `resultsPerPage`, default 100), so it was ignored and every run pulled 8 hashtags × 100 = 800 videos × $0.004. Empirically confirmed before deploy: a test run with `resultsPerPage:5` charged exactly 5 results ($0.02). Fixed config (8×20) = 160 results ≈ $0.64/run (−80%). Instagram already used the correct `resultsLimit: 20` (160 reels, ~$0.37) — left unchanged. | CONTEXT: Fix applied to `deploy-halo-intelligence-report.js`, redeployed live (PUT 5ZqTSaUEtxnAndiY), and verified via GET — TikTok node now sends `resultsPerPage:20` with no `maxPostsPerPage`; workflow active. Same latent bug fixed in `projects/crave-outreach/src/scrape.py` `_build_run_input` (dead branch — crave-outreach config uses `clockworks/tiktok-scraper`, not the hashtag actor — fixed for correctness). Docs synced: deploy-script comment, WORKFLOWS.md (Workflow 15 runbook row + new "Scraping Volume & Cost" table), skill `.claude` + `.agents`. NOT changed: the `&limit=50` on the Apify URL only truncates rows returned to n8n, not what the actor scrapes/bills (optional future tweak: raise it to use all 160 paid results, currently only first ~50 reach the scorer). Transcript add-on stays off (bills per video-minute). Crave-outreach's own `clockworks/tiktok-scraper` usage ($0.34 / 7 runs) is well-controlled. Verification test run cost ~$0.02. Next real run (Mon 7AM PHT) should bill ~$1.01 total vs the prior $3.57.

---

[2026-06-12] DECISION: Invoice email tone is BROAD/stage-agnostic by default — kickoff or completion flavor only on an explicit signal; "retainer"/"starter pack"/"onboarding" are NOT kickoff signals. | REASONING: Noa flagged the Little Saints email (INV-VJHIYE8S-0002, 2 months into the engagement) opening with "Super excited to be kicking things off" — second tone miss she's caught. Two compose paths shared the flaw: krave-bot system-prompt.js offered ONLY kickoff/completion templates (anything not a wrap defaulted to kickoff excitement), and ops-dashboard composeInvoiceEmailBody listed "retainer" + "onboarding" as kickoff trigger examples, with the suggestive "e.g." letting Claude read "Starter Pack" as kickoff too. Neither path checks engagement history, so broad-by-default is the only fail-safe: a neutral email is never wrong. Noa's explicit ask: "maybe just make it broad?". | CONTEXT: Fixed in both paths — krave-bot kickoff/completion flavor now requires the requester to explicitly say deposit/kickoff or final/completion in the Slack request; dashboard requires the literal words "deposit"/"kickoff" (or "final"/"completion"/"balance") in the project description, with an explicit instruction that retainer/starter-pack/onboarding names do NOT count. Verified no tone logic anywhere else: deploy-invoice-approval-polling.js only builds the payload (compose_body delegated to the dashboard endpoint), client-invoice-creation SKILL.md x2 and SOP describe no body tone. Both services auto-deploy on push to github master (Render: krave-ai + ops-dashboard).

---

[2026-06-12] DECISION: Creator Invoice Email Scan (`DbIJYYQ3FE4HKprB`) REACTIVATED with John's explicit approval, same day as the fix. | REASONING: Incident class is closed at three independent layers (PDF-only intake, is-invoice classification, known-sender gate); worst case for an unknown sender is now an #ops-command flag, never an outbound email. | CONTEXT: `active: true` verified via API. First live runs at the next 09/12/15/18 PHT cron slots — watch #ops-command not-invoice notices for classification quality. Plain-language incident summary drafted for John to send Noa on WhatsApp.

---

[2026-06-12] DECISION: Creator Invoice Email Scan FIX deployed (options 1+2+3, John-approved) — workflow updated live but left INACTIVE pending John's explicit activation. | REASONING: Four guards address every root cause of the morning's misfire: (1) PDF-only intake — `filename:pdf` in the Gmail query AND the extractor rejects images, so inline screenshots can never enter the pipeline; (2) real classification — Claude's system prompt now asks is_invoice (invoice issued TO Krave vs proposal/quote/pricing/receipt/contract/outbound invoice) using email sender+subject context, and Parse & Validate requires `is_invoice === true` (parse failure fails safe to not-invoice); (3) per-message reply dedup — new Dedup Reply Gate / Dedup Notice Per Message code nodes collapse N attachments into one reply/flag; (4) known-sender gate — missing-bank-details auto-reply only for @kravemedia.co senders or vendors already in the Bills tracker (column B, via the Fetch Existing Bills range widened to B:I); unknown senders get NO email — #ops-command flag + "On hold — missing bank details" tracker row (which also feeds messageId dedup) + mark read. Bonus guard: not-invoice skips now post a one-line #ops-command notice instead of marking read silently (the incident also buried the lead's real questions). | CONTEXT: Deployed via PUT only — the deploy script's auto-activate was deliberately NOT run; the permission classifier also blocked it, and activation is John's call. Verified live via GET: all 7 new nodes present, PDF-only query, is_invoice + email context in prompt, B:I fetch, correct branch wiring; 30 nodes total. Dedup Filter updated for the wider range (messageId = index 7). Docs synced: WORKFLOWS.md (index row + full section 22 rewrite), README.md row, SKILL.md x2 banners, SOP KM-SOP-001 banner, deploy script header. To go live: activate `DbIJYYQ3FE4HKprB` in n8n (or POST /api/v1/workflows/DbIJYYQ3FE4HKprB/activate); next cron fires at the next 09/12/15/18 PHT slot. Suggested first-run check: watch #ops-command for not-invoice notices to confirm classification quality.

---

[2026-06-12] DECISION: Creator Invoice Email Scan workflow (`DbIJYYQ3FE4HKprB`) DEACTIVATED after a client-facing misfire; do not reactivate until classification + reply guards are rebuilt. | REASONING: At 9:00 AM PHT (execution 8041) it picked up Guy Nagler's reply on the OKOA sales thread ("Re: OKOA x Krave Media", guy@zyg.com, ~$18k+ lead), treated two inline pricing screenshots (image.png) as creator invoices (Claude extracted "amounts" $23,100 / $31,200 from the proposal pricing), found no bank details, and sent the "missing bank details" auto-reply TWICE from john@kravemedia.co — unprofessional, client-visible, Noa flagged it. Root causes: (1) Gmail query `(invoice OR bill OR creator OR payment) + filename:png/jpg` matches sales threads with inline images; (2) `isInvoice = creatorName && amount > 0` accepts any priced document — the parser is never asked IF it's an invoice; (3) the reply node fires once per attachment, not per message → duplicate emails; (4) the missing-bank-details path auto-replies externally with no known-sender check and no human approval. | CONTEXT: Workflow deactivated live (active:false verified). John sent a "please ignore, automation error" reply on the thread at 9:19 AM. Disabled status synced across WORKFLOWS.md, README.md, both SKILL.mds, SOP KM-SOP-001, and the deploy script header (deploy() auto-activates — do not run it). Fix requirements before reactivation: image attachments excluded or inline-disposition filtered, explicit is-this-an-invoice classification with thread-context awareness, replies deduped per message, external replies gated on known creator/strategist senders or routed to John for approval.

---

[2026-06-09] DECISION: Contract-generation flow finalized + PandaDoc API stays MANUAL. Term fields (effective date, # Rounds, Initial Package) are blank-by-default fill-in lines that Noa completes; the automation only writes a value when deliberately given one. Flow: Noa pings John → John runs `/contract-generation` → skill generates the .docx → John sends to Noa for approval → approved: John uploads to PandaDoc + sets sig fields + sends; not approved: John relays feedback, iterate (data → deals/<slug>.json; wording → reconstruct-template.js). Created SOP `references/sops/contract-generation.md` (KM-SOP-008). | REASONING: John clarified Noa fills date/rounds/party/sig herself and is the approver; John is the operator. PandaDoc API researched (June 2026): API inside the team's own workspace requires the Enterprise plan (the "top tier" Noa rejects); a standalone API Developer plan (~$40/mo, ~40 docs/mo) + free sandbox (~60/yr) exists but lands docs in a separate silo, not Noa's sending workspace — so it doesn't remove her manual step. Low/ad-hoc volume doesn't justify Enterprise. | CONTEXT: Keep manual upload; revisit only if volume grows or Krave moves to PandaDoc Enterprise (the deal-JSON model can then feed an API call with no rework). Zenwise custom contract generated (output/zenwise-retainer-20260609.docx) pending Noa's feedback. Sources: pandadoc.com/api/pricing, pandadoc.com/pricing.

---

[2026-06-09] DECISION: Contract template is RE-AUTHORED from the start PDF (the selection-based "PANDADOC READY" master: deliver N Rounds + Initial Package selection + full Appendix A + Terms & Conditions) via `reconstruct-template.js` using the `docx` library, because no clean editable .docx of that PDF existed (the same-named .docx was a customized one-off, and RETAINERS.docx had no Appendix A — John required Appendix A "like the PDF"). Both contract shapes now render end-to-end. | REASONING: John needs Appendix A present and chose reconstruction over using the mismatched files. Authoring from the PDF text gives one reviewable source for the legal wording and bakes in placeholders + the custom conditional cleanly (no fragile XML surgery on a 3rd-party doc). Field model: effectiveDate / numRounds / initialPackage (+ isCustom → monthlyFee, deliverables[], performanceTiers[] for the {#isCustom} "2.1a Custom Package Pricing" block). Party/signature fields stay blank for John's PandaDoc setup. | CONTEXT: Verified: `node reconstruct-template.js` → template; standard deal (example-standard.json) renders Appendix A + no custom block; custom deal (zenwise.json) adds the custom-pricing section and keeps Appendix A; both valid docx, zero leftover placeholders, full T&C + clawback present. master/ holds the start PDF as visual source-of-record. Supersedes the two earlier 2026-06-09 entries (the initialPackage plan, then the RETAINERS standard-only build) — RETAINERS was the wrong master. IMPORTANT: this is a re-authored legal document — John must review the rendered .docx in Word against the PDF before first real use; clause fixes go in reconstruct-template.js, never the generated .docx. Deferred: PandaDoc API draft-creation; baking PandaDoc signature tokens into the template.

---

[2026-06-09] DECISION: Set the canonical contract master to `MASTER CONTRACT - RETAINERS.docx` (a clean fill-in retainer), NOT the `MASTER CONTRACT - PANDADOC READY.docx` John first dropped — that file turned out to be an already-customized Zenwise one-off (base fee + performance hardcoded, only sig/date blanks left, and stale numbers: $2,500 vs the $2,000 negotiated). Built the STANDARD per-Round shape end-to-end and verified it; the custom/performance shape is deferred. | REASONING: The PANDADOC-READY file had nothing left to automate and would have baked Zenwise's terms into every contract. RETAINERS.docx has each blank in its own text run, so `build-template.js` injects placeholders deterministically (reproducible, no hand-editing) — chosen over manual Word templatizing. The master has no Appendix A; it's a fill-in-deliverables retainer, so the fields are numRounds/numAds/numConcepts/numHooks/numCreators/feePerRound (NOT the initialPackage model from the earlier plan entry above). | CONTEXT: `node build-template.js` → template; `node generate-contract.js --deal deals/example-standard.json` → verified valid docx, no leftover placeholders, all values correct. Party/signature fields left blank for John's manual PandaDoc setup. `generate-contract.js` rejects `isCustom:true` until the custom branch is built. John chose "one template, both shapes" — custom/performance branch (conditional `{#isCustom}` clauses, modeled on the Zenwise doc's language) needs careful legal authoring + his review, so it's the next step. Pending: John opens the generated docx in Word to confirm formatting before first real use.

---

[2026-06-09] DECISION: Built the `contract-generation` skill + local generator (`projects/contract-generation/`) to fill the master retainer .docx from a deal's commercial terms into a new PandaDoc-ready file. The automation fills ONLY commercial terms (effectiveDate, numRounds, initialPackage, and custom base-fee + performance clauses); party/signature fields (brand name, BR number, signatory name/position, sign dates) are left blank for John to set up manually in PandaDoc. | REASONING: Contract creation was fully manual (Noa pings → hand-fill Word → upload). docxtemplater + pizzip on a placeholder-ized copy preserves the lawyer-approved layout byte-for-byte and handles custom performance deals (e.g. Zenwise "2K base + 1x ROAS") via {#isCustom} conditional sections — chosen over rebuilding the doc (legal/formatting risk) or PandaDoc API (no credential yet). John scoped party/signature fields out of the automation. | CONTEXT: MVP outputs a .docx for manual PandaDoc upload; master is never modified; deals/ + output/ are gitignored (local-only). Two mandatory human checkpoints (confirm terms → review before upload). Pending: John drops the editable master into master/ and placeholder-izes template/retainer-template.docx by hand in Word (the master has no PandaDoc tokens, so sig fields are still placed manually). Deferred Phase 2: PandaDoc API draft-creation reusing the same deal JSON, and baking PandaDoc signature tokens into the template.

---

[2026-06-08] DECISION: Aligned Halo Weekly Intelligence tracker columns to Alleah's manual inspo-sheet format, surfaced the Posts sheet in the "Halo Post Inspiration Library" table of the Ideas & Moodboard Slack canvas (F0A2ATP4D5L), and added Baste to the report email + sheet access. | REASONING: Alleah's first-report feedback — wanted the tracker accessible to the whole team in the existing moodboard, in a format consistent with the sheet she pulls manually every Friday, and shared to Baste for ads inspo. The automated tracker now owns TikTok (reliable scrape); Alleah re-focuses her manual pulls on Instagram (harder for AI to scrape). | CONTEXT: Added 5 Claude-generated columns (Keyword, Format, Visual Style, Hook Type, CTA / Ending) with Hook Type/Format constrained to Alleah's "🪝 Hook Type Legend" taxonomy; renamed Hook→Hook (0–3s), Why It Performed→Why It Works, Halo Angle→Halo Adaptation, URL→Post URL; dropped Transcript (scraper unreliable). Redeployed workflow 5ZqTSaUEtxnAndiY. Known blemish: Slack canvas API leaves an empty table shell above the library table that cannot be removed via slack_update_canvas without deleting the real table — needs 1-click manual delete in Slack.

[2026-04-08] DECISION: Paused Osome reconciliation automation build. | REASONING: Too many upstream blockers to make meaningful progress — Airwallex MCP needs Admin API key + transfers endpoint, takhelnoa@gmail.com auth failed, n8n not yet built. Manual SOP + Osome ingestion email are in place; automation resumes when blockers are cleared. | CONTEXT: 700 transactions remaining (Dec 2024–Dec 2025), deadline end of April 2026 for Eclipse Ventures tax submission.

**Osome automation status snapshot (2026-04-08):**
- Working: SOP documented (FIN-001), skill built, Osome ingestion email confirmed (977e06fe7c21-628067@my.osome.com), noa@kravemedia.co Gmail MCP connected
- Blocked: Airwallex MCP (401 — needs Admin API key), Airwallex transfers endpoint missing from MCP, takhelnoa@gmail.com not connected
- Next actions to unblock:
  1. Noa generates Admin API key in Airwallex → Settings → Developers → replace key in .mcp.json
  2. Add `list_transfers` + `get_transfer` endpoints to mcp-servers/airwallex/index.js
  3. Set up Gmail forwarding filter in takhelnoa@gmail.com → noa@kravemedia.co
  4. Build n8n workflow: Airwallex API → confirmation letter PDF → email to Osome ingestion address

[2026-05-07] DECISION: Deleted insense-creator-outreach skill, projects/insense-creator-database, SOP, and superpowers spec/plan | REASONING: Standalone Playwright runner approach is being scrapped; will replan around Playwright MCP | CONTEXT: User decision before kicking off fresh design

[2026-05-07] DECISION: Insense outreach v2 uses Playwright MCP + two skills (insense-triage headless cron, insense-send manual CDP attach) | REASONING: Old standalone Node runner was heavy (separate project, login each run, Slack approval thread the operator skipped in practice); MCP-driven flow with operator-attached Chrome removes the auth/dedup ceremony | CONTEXT: Old projects/insense-creator-database/ deleted same day, template recovered from git. Cache + template under data/insense/, scheduling via /schedule per-campaign

[2026-05-07] DECISION: Insense outreach v3 collapsed to a single combined skill `insense-outreach` (replaces v2 split of `insense-triage` + `insense-send`) using Playwright Extension MCP attach to operator everyday Chrome | REASONING: Operator workflow is "view application, if pass send right away" — the Slack approval gate added a step the operator did not actually use, and CDP-port-9222 launcher was friction (close-all-Chrome-first). Playwright Extension lets MCP attach to normal browser; combined skill matches actual ritual; defaults to dry-run for safety | CONTEXT: Validated extraction end-to-end on Little Saints campaign 2026-05-07; 4 candidates passed filter (elevatewithdai, ricthelisouza, byeri.ugc, ugc.nonie). Selectors and regexes recovered + corrected (anchor-walk strategy not ancestor-walk; lowercase "finished deals"; value-before-Engagement-rate label)

[2026-05-07] DECISION: Insense outreach v3.1 — collapse the per-candidate walk into a single browser_evaluate with in-page async loop | REASONING: Previous per-step MCP pattern (~10 roundtrips per candidate at ~1.5s each) was the bottleneck; the page itself runs each candidate in ~5s, so per-candidate cost was 90% MCP roundtrip overhead. Single in-page evaluate eliminates that. | CONTEXT: Validated on 3 Little Saints candidates 2026-05-07 — 17s total vs ~10 min projected with old pattern (~25x). Driver still does pre-filtering (cache lookup, prev-collab block, card-ER<1% short-circuit) before passing targets to the walker. Canonical script lives in .claude/skills/insense-outreach/SKILL.md.

[2026-05-07] DECISION: insense-outreach v3.2 — added staleness guard + drawer-load retry; confirmed Insense hard-rate-limit past ~280 cards/150 sends per session | REASONING: Run 9 sends went out but with stale-drawer values cached (all FD=2), risking incorrect filter decisions. Run 10 with patched logic correctly refused all 10 stale-drawer attempts (0 false positives). Insense throttle is session-scoped — recommend ending session past 150 sends and resuming fresh next time | CONTEXT: Day 1 totals: 147 invites sent across 9 runs on Little Saints (~28% of 521 applicants). Skill canonicalized in .claude/skills/insense-outreach/SKILL.md.

[2026-05-07] DECISION: Added Client Payment Reply branch (v6) to Krave Payment Detection workflow `NurOLZkg3J6rur5Q` | REASONING: Nutrition Kitchen 2/2 (INV-QIWVIG7H-0001, 4975.65 USD) silent-missed because payment landed in Eclipse Ventures Pte. Ltd. (non-Airwallex SG bank) and client confirmation came as a reply on the invoice thread — neither the airwallex.com Gmail filter nor the Airwallex API poll could see it. New branch `Search Client Replies` + `Parse Client Replies` adds a third detection path that emits Needs Review payloads only (never auto-marks paid). Match rule: phrase signal (e.g. "payment is done", "transfer details") AND tracker fuzzy match by sender domain. Idempotency via separate `processedClientReplyIds` (last 200). Slack volume capped at 10/run. | CONTEXT: Surgical patch via REST PUT preserved credentials. Pre-patch snapshot `n8n-workflows/snapshots/payment-detection-pre-client-reply.json`. Patch script `n8n-workflows/patch-payment-detection-client-reply.js`. Spec `docs/superpowers/specs/2026-05-07-payment-detection-client-reply-branch.md`.

[2026-06-08] DECISION: Halo Weekly Intelligence Report (`5ZqTSaUEtxnAndiY`) — fixed Instagram pipeline + platform-specific gates + sheet append, switched live channel to Halo Home | REASONING: Run 7433 succeeded but shipped TikTok-only. Instagram Apify body omitted `resultsType`, so the actor defaulted to `posts` (static photos, <=4 likes) and all 50 were filtered out; the Sheet `Posts` tab stayed empty because `Prepare Sheet Rows` read `$json.sheetRows` off the upstream Gmail node's response instead of `$('Format Report')`. Fixes: Instagram `resultsType: 'reels'`; platform gates (TikTok >=5,000 likes, Instagram >=10,000 views — IG reels earn thousands of likes, not millions); dropped the `comments >= 50` escape; enforce video/reels-only; sheet append reads Format Report. Channel moved from testing #ops-command (C0AQZGJDR38) to Halo Home C0A22NPLV38. | CONTEXT: Validated live via manual run 7474 — TikTok 50->10, Instagram 50->10 (real reels), 20 rows written to Posts tab, Slack both sections + email delivered. Redeployed via `node n8n-workflows/deploy-halo-intelligence-report.js` (REST PUT preserves credentials). Report + non-technical launch announcement posted to C0A22NPLV38 as John AI. Docs synced (.claude + .agents SKILL.md, WORKFLOWS.md, README.md). Known gap: documented filters "last 14 days" and "min 3 niche categories" remain unimplemented in code.


[2026-06-10] DECISION: Full setup audit + remediation — Airwallex key rotated, timezone canonicalized to Asia/Manila, skill parity restored, anti-staleness guardrails installed | REASONING: Multi-agent audit found (1) live Airwallex creds committed in .claude/settings.json and publicly readable in withally-workshop/claudevasystem since ~Apr 20 — key rotated in dashboard, all consumers updated (n8n Payment Detection / Invoice Request Intake / Invoice Approval Polling via patch-airwallex-key-rotation.js; Render krave-ai env var; .mcp.json; .env), repo scrubbed (deploy-payment-detection.js -> process.env, workflow export redacted); (2) doc-sync PostToolUse hook never fired due to invalid args-array registration — fixed to single command string; (3) three timezone labels in conflict (PHT/ICT/SGT, a real one-hour ambiguity in the deep-work block) — Asia/Manila (PHT, UTC+8) is now canonical everywhere; (4) three Halo workflows (Daily Digest, Inventory Alert, Weekly Report) fired at 1-2 AM Manila because UTC-intended crons ran in workflow tz Asia/Manila — fixed live to 10 AM / 9 AM / 9 AM Mon PHT; (5) skill parity broken for 6 of 21 skills — .agents counterparts created and registered; (6) CLAUDE.md stale on every inventory — rewritten inventory-free with a Source-of-Truth table. | CONTEXT: Guardrails now active: pre-commit BLOCKS credential literals (slack/shopify/anthropic/render/apify/96-hex patterns) and warns on parity drift, unregistered skills, deploy/doc mismatch, and ICT/SGT/Bangkok labels on every commit; .claude/rules/doc-freshness.md bans doc inventories. Commits d64df26..89e00e9 pushed to github (private) only — origin (withally-workshop) is PUBLIC, must never be pushed, and is pending takedown/private by John. Also pending John: revoke old Shopify token shpat_7b37... if still active. Old Airwallex key confirmed dead. Hygiene: .playwright-mcp logs (29.6 MB) and archives/caveman deleted locally; mangled-path junk removed from index.

[2026-06-10] DECISION: Throttled `Krave — Price Reply Auto-Resubmit` (`nzFTk4e9NRi6Jk9r`) from 24/7 `*/10 * * * *` to `*/30 8-19 * * 1-5` (Asia/Manila); deactivated then re-activated | REASONING: n8n cloud monthly execution cap (2,500) was ~2,000 consumed; investigation of `GET /api/v1/executions?limit=250` showed this one workflow = 160 of the last 250 runs (~144/day, ~4,300/mo on its own — the sole cause of the overage). It polls the full history of PRIVATE channel #payments-invoices-updates (C09HN2EBPR7) every 10 min around the clock to catch occasional strategist price replies — grossly over-provisioned for the event rate. Throttle to business-hours weekdays drops it to ~530/mo (8x cut) while keeping <=30 min resubmit latency. | CONTEXT: Stopped immediately via POST /deactivate, then redeployed via `PRICE_REPLY_RESUBMIT_WORKFLOW_ID=nzFTk4e9NRi6Jk9r node deploy-price-reply-auto-resubmit.js` (PUT preserves creds) and re-activated; verified live (active=true, cron `*/30 8-19 * * 1-5`, webhook krave-price-reply-resubmit intact). Workflow already carries a parallel Webhook Trigger but NOTHING fires it (referenced only in docs/deploy script). TARGET (needs John, one-time, can't automate): #payments-invoices-updates is PRIVATE so its events ride `message.groups`, which the Slack app does NOT subscribe to (#ops-command/approvals is public → `message.channels`, already wired). To go fully event-driven (~0 idle execs): John adds `message.groups` bot event in api.slack.com + reinstalls app, then extend the existing Approval Reply Trigger router to fan out to krave-price-reply-resubmit and drop the Schedule Trigger. Also flagged: two unrelated workflows erroring 100% — LinkedIn Resource Post Alert (`Rw2VZ6sAzAhJteyJ`, 17/17) and FreelanceLeadsPro V1 (`W01MhWiz7qxnKCYR`, 14/14). Docs synced: WORKFLOWS.md row 18, README.md row, deploy-price-reply-auto-resubmit.js inline comment.

[2026-06-15] DECISION: Crave creator outreach went LIVE on Smartlead Base ($39/mo, no API) — manual UI flow + new `export_approved.py`; documented as KM-SOP-009 with a weekly cadence | REASONING: Smartlead's 14-day trial (key created 22 May) expired ~5 June; API access (n8n `Crave - Daily Lead Push` ke52OLrSUXk8mPVw / `Crave - Status Sync` uUGxA3GW1W0vq6el, and `smartlead.py` push/sync/stats) requires Pro ($94/mo) — confirmed by Smartlead's in-product assistant and the API 401ing on every endpoint except `GET /campaigns`. On Base everything runs in the Smartlead UI. Built `projects/crave-outreach/src/export_approved.py` (reads Sheet via Google creds, writes a Smartlead-ready CSV of `status=approved`+email rows, marks them `outreach_queued` to prevent re-export). Campaign 3375376 launched today: 88 approved leads imported (of 116; 28 had no email), verification split 36 valid / 5 invalid / 47 unknown, sending to valid+unknown at ~25/day Mon–Fri America/New_York from hello@joinkravemedia.co (From Name Mimi). Email copy = Noa-approved template (Slack DM 2026-05-25 "this is good to go!"), Typeform CTA https://form.typeform.com/to/lAPIxgqv, stored in src/outreach.py. | CONTEXT: Cadence is weekly — scrape + Noa approves + export + import into the SAME campaign (one mailbox = ~25/day shared across campaigns, so no parallel campaigns; ~125/week capacity; add a second warmed mailbox to scale, not more campaigns). Conversions flow through the Typeform which John can't see — Noa/strategist must monitor submissions. The 28 no-email creators stay parked as `approved` (NOT handed to Alleah — she's Halo Home, not Krave Media). Launch reported to #ops-command as John AI. Docs synced: KM-SOP-009 (references/sops/crave-creator-outreach.md), .claude + .agents crave-outreach SKILL.md, projects/crave-outreach/README.md. Re-activate n8n workflows + smartlead.py when upgraded to Pro.

[2026-06-10] DECISION: Deactivated two orphaned, 100%-failing n8n workflows — FreelanceLeadsPro V1 (`W01MhWiz7qxnKCYR`) and LinkedIn Resource Post Alert (`Rw2VZ6sAzAhJteyJ`) | REASONING: Execution-cap investigation found both active=true with live schedule triggers (every 2h 24/7; :00/:30 hrs 8-13 Sun-Fri) erroring on EVERY run at their first external fetch — FreelanceLeadsPro "Fetch Paid Orders" = dead host, LinkedIn Resource Post Alert "Fetch Resource-Promo Tasks" = bad request. ~360 + ~312 = ~670 failed execs/mo wasted against the 2,500 cap, producing nothing. Neither is in WORKFLOWS.md (not part of Krave/Halo ops stack) — orphaned experiments. User believed they were unpublished; they were not. | CONTEXT: Deactivated via POST /api/v1/workflows/{id}/deactivate (reversible). Errors are at-source (dead endpoints), so revival needs new data sources, not a node fix. Pending user decision: delete vs keep. NOTE on Halo: user asked if Halo AI bot was a big consumer — it is not; Halo Home - Slack Bot = 54 execs (webhook-driven, scales with use), other Halo workflows <=8 each.

[2026-06-10] FINDING: Slack Events architecture — Slack delivers ALL events to the krave-bot Render service (`/slack/events`, Bolt ExpressReceiver), NOT to n8n | REASONING: While scoping the price-reply event-driven conversion, confirmed the n8n "Approval Reply Trigger" (`arUrmWEgjzuVc27Y`) is fed by the bot, not Slack: bot's message handler forwards ONLY #ops-command (C0AQZGJDR38, approvals) to webhook krave-approval-reply-trigger via forwardToN8n() (projects/krave-bot/server.js:236-250,273-274); all other channels are received then dropped with an early return. Verified empirically: across 30 router executions, 150 events were channel_type=channel from C0AQZGJDR38, zero from private #payments-invoices-updates (C09HN2EBPR7) despite that channel having human traffic on 06-09. | CONTEXT: Implication for zero-cost price-reply: if Slack app has message.groups (John says yes), the bot already RECEIVES C09HN2EBPR7 messages at server.js:269 and discards them — so the conversion is a bot-side branch (forward C09HN2EBPR7 human messages to krave-price-reply-resubmit webhook), then delete the n8n Schedule Trigger. Unconfirmed: whether message.groups is actually delivering (Bolt logs events at DEBUG; bot runs INFO). Plan: ship bot branch + confirm log, keep throttled schedule as fallback, strip schedule after a live reply proves delivery.

[2026-06-10] DECISION: Krave Price Reply Auto-Resubmit (`nzFTk4e9NRi6Jk9r`) is now FULLY event-driven — Schedule Trigger removed | REASONING: Confirmed message.groups delivers private-channel events to the krave-bot: after deploying the bot branch (server.js forwards #payments-invoices-updates C09HN2EBPR7 human messages to the krave-price-reply-resubmit webhook), n8n logged 5 mode=webhook executions (all success: 04:47 post-deploy + John's test replies 05:01 x3 / 05:10) vs the schedule's mode=trigger runs. End-to-end chain verified: Slack message.groups -> krave-bot /slack/events -> forwardToN8n(price-reply path) -> n8n webhook trigger. | CONTEXT: Removed the scheduleTrigger node + connection from deploy-price-reply-auto-resubmit.js, redeployed via PRICE_REPLY_RESUBMIT_WORKFLOW_ID=nzFTk4e9NRi6Jk9r (PUT preserves creds), verified live: active=true, only Webhook Trigger remains, schedule present=false. Net journey: 24/7 */10 (~4,300 execs/mo, the cap overage) -> throttled */30 8-19 * * 1-5 (~530/mo) -> webhook-only (~0 idle execs, fires only on real replies). Docs synced: WORKFLOWS.md row 18, README.md. See [[reference_slack_events_architecture]].

[2026-06-10] DECISION: Fixed Noa's Gmail Airwallex filters — client invoice payments now route to _Payment_Received (inbox), all other Airwallex deposits to _Airwallex (skip inbox) | REASONING: John reported client payment deposit emails (e.g. AMPLIFIED 1,467.45 SGD) were missing from inbox AND every label/folder. Root cause: (1) a LIVE bare catch-all filter `from:support@info.airwallex.com → REMOVE[INBOX], no label` archived ALL Airwallex notifications nakedly into All Mail; (2) the intended `_Payment_Received` labeling filter was never created (label ID defined in setup-filters.js KNOWN_LABELS but unused — the closing summary falsely claimed it existed). So client payments were archived and unlabeled. Label real name is `_Payment_Received` (Label_5194298534623747326), leading underscore. | CONTEXT: Fix via service-account (mcp-servers/gmail, gmail.settings.basic + modify, subject noa@kravemedia.co): deleted bare catch-all; created client-payment filter `"You received a deposit" -"STRIPE PAYMENTS SINGAPORE" -SHOPIFY -"deposit from AIRWALLEX" -"WISE ASIA"` → _Payment_Received, stay in inbox; added _Airwallex skip-inbox filters for Shopify / Airwallex-internal / Wise deposits (Stripe filter already existed — Shopify Payments rides Stripe rails so most Shopify payouts already hit it). Backfill: labeled 189 historical non-Stripe deposits, then reclassified 39 non-client (32 Airwallex-internal/FX, 6 Wise, 1 Noa personal) to _Airwallex out of inbox; 150 genuine client payments remain in inbox+_Payment_Received (incl. 6 round-USD ones the source-parser initially missed: LLAMA, IM8, SUPERPOWER HEALTH, NOOCI). Verified AMPLIFIED 1,467.45 SGD now = INBOX+_Payment_Received. Gmail has no "else" so non-client sources are enumerated explicitly. setup-filters.js synced. Note: 150 historical client payments (mostly old/reconciled) now sit in inbox per John's "stay in inbox+label" rule — not trimmed; revisit if inbox noise becomes an issue.

[2026-06-10] DECISION: Inbox Triage (`EuT6REDs5PUaoycE`) — keep client payment emails in the inbox (never archive `_Payment_Received`) | REASONING: After routing Airwallex client deposits to inbox + `_Payment_Received`, the daily triage would have undone it: it archives every non-`EA/Unsure` email out of the inbox, and client payments classify as `EA/FYI` → would be pulled back out. Fixed the "Archive?" IF node to archive only when `tier != EA/Unsure` AND NOT `labelIds` includes `Label_5194298534623747326` (_Payment_Received). Payments now classify FYI (no draft), show once in the summary, and stay in the inbox. | CONTEXT: Surgical REST PUT (preserved Gmail cred vxHex5lFrkakcsPi; MCP update_workflow would drop it) + re-activate; verified active=true, condition + cred intact. Pre-patch snapshot n8n-workflows/snapshots/inbox-triage-pre-payment-archive-fix.json (gitignored). Also pre-labeled the 150 backlog `_Payment_Received` in-inbox emails with `EA/FYI` so they skip triage (excluded by `-label:EA/FYI`) instead of flooding the next summaries — kept in inbox, INBOX not removed. Note: API PUT rejects extra settings keys (availableInMCP/binaryMode) — whitelist to executionOrder/timezone/etc. Synced: deploy-inbox-triage-daily.js + SKILL.md (.claude + .agents). See [[reference_gmail_airwallex_filters]].

[2026-06-11] DECISION: Payment Detection v7 — confidence-gated, verification-guarded Airwallex mark-as-paid (re-introduced after v4 removal) | REASONING: 2026-06-10 AMPLIFIED MARKETING INV-AG2H2WFA-0001 (1,467.45 SGD, medium-client exact-amount match) updated the tracker to Payment Complete but silently left Airwallex UNPAID — the v4 over-correction removed mark-paid entirely with no manual-step notification. John decided: auto-mark when confidence is high, flag manual otherwise. Gate (awMarkEligible in matcher): full non-Osome payment AND (invoice-number match OR medium-client with single payment settling exact full amount). Guard (new node): re-fetch live Airwallex invoice, require currency + total_amount match vs tracker (±0.01) and payment_status not already PAID/void; any mismatch/auth-fail/API-error → no write + "NEEDS MANUAL mark-as-paid — reason" in the Slack ✅ alert (which now always carries an "Airwallex:" status line). Guard directly addresses the WELLE failure mode: a wrong match cannot mark a different-amount invoice. | CONTEXT: Applied via patch-payment-detection-aw-markpaid.js (idempotent ensure-state, --dry-run/--apply) to NurOLZkg3J6rur5Q; verified active + clean webhook run (exec 8000). Auth via NEW n8n credential "Airwallex API (login headers)" (httpCustomAuth, Ry37bj6SFVD1zcd0) on an HTTP Request node — no secrets in workflow code (unlike legacy n17, which should migrate to this credential at next key rotation). HARD CONSTRAINT discovered: n8n cloud Code nodes lack this.helpers.requestWithAuthentication (only this.helpers.httpRequest) — the v6.1/v6.3 requestWithAuthentication calls in n3 are inside silent try/catch and likely dead code; auth must live in regular nodes. v1 of this patch shipped broken for ~10 min (failed closed to manual flag — safe) before the auth-node fix. AMPLIFIED invoice itself marked paid via MCP after John's explicit approval (verified UNPAID/1,467.45 SGD first). Synced: SKILL.md (.claude + .agents), WORKFLOWS.md (purpose, node flow, v7 hardening note, outputs, error handling, Airwallex credential section — also removed a hardcoded client-id literal), README.md, payment-detection.md stale banner, deploy-payment-detection.js header, SOP client-invoice-creation.md Step 6.

[2026-06-11] DECISION: Alleah-feedback round — refund scope pinned to May 1–Jun 10 pending Noa's date-range call; Seal $109 fix delegated to Alleah via DM | REASONING: Shopify-verified worklist (49 orders, $264.84 SGD) delivered to Alleah in #halo-home-shopify; our app token is read-only so refunds are manual on her side. Scan also found 20 more eligible orders Apr 14–30 ($100) and a hard blind spot before Apr 12 (app's read_all_orders scope NOT effective — 60-day rolling window only); John is asking Noa via WhatsApp how far back to extend. $109 checkout display root-caused to Seal plan 691661832514 (fixed $109 every cycle, no first-order restriction) — display-only, renewals bill $33 correctly; fix instructions DM'd to Alleah as John. Mimi stopgap explaining $109-vs-$33 shipped + verified live (4d5ea74). | CONTEXT: Full state snapshot in projects/halo-home-chat/STATUS-alleah-feedback.md — read that first when resuming this work.

[2026-06-11] EVENT + ALIGNMENT: Airwallex Spend Bills API released (create + mark-as-paid) — MCP server aligned to published spec; still blocked on API-key Spend scope | REASONING: Bing Quek (Airwallex Product Strategy) emailed John 2026-06-11 12:29 PHT: `POST /api/v1/spend/bills/create` and `POST /api/v1/spend/bills/{id}/mark_as_paid` went live today (follow-up to the Jun 8 customer interview; beta access was the blocker, not account tier). Our pre-built airwallex_create_bill deviated from the published spec — fixed: `currency`->`billing_currency`, added required `tax_status` (default TAX_EXCLUSIVE) + `sync_status` (NOT_SYNCED), line-item quantity/unit_price now strings, attachments now array of file-id strings (was object wrap), legal_entity_id now required per spec. New tool airwallex_mark_bill_paid (bills/AP) added — distinct from airwallex_mark_paid (invoices/AR). | CONTEXT: Live probe of GET /api/v1/spend/bills returns 401 "Insufficient permissions" both with and without x-on-behalf-of — auth succeeds, so the API key lacks the Spend scope. PENDING JOHN: (1) enable Spend/Bills scope on the API key in Airwallex dashboard, then re-test; (2) decide whether creator-invoice flow switches from forward-by-email to API bill creation. Until both, forward-by-email remains the operative path — "do not call" directives kept (reworded) in creator-invoice-processing SKILL.md (.claude + .agents) and SOP creator-invoice-management.md. WORKFLOWS.md/README.md/deploy script/krave-bot prompt verified — describe behavior, no stale claim. Tools NOT live-verified (blocked on scope); code matches published spec only.

[2026-06-11] DECISION: Airwallex Spend 401 root-caused — Spend API requires a Scoped API key with ORGANIZATION-level permissions, which our account-level key cannot carry; MCP server now supports a second Spend-only key | REASONING: Follow-up probes on the release-day endpoints themselves (POST /spend/bills/create with empty body) returned 401 not 400 — authorization, not validation or unreleased routes. Airwallex support then confirmed the requirement: create a Scoped API key, tick Spend resources Read/Write under Organization-level permissions. Two-key design chosen over rotating the main key: main key keeps serving billing/invoices/n8n/Render untouched (zero blast radius), new org-scoped key serves only /api/v1/spend/*. | CONTEXT: mcp-servers/airwallex/index.js: per-key token caches; airwallexRequest routes /api/v1/spend/* through AIRWALLEX_SPEND_CLIENT_ID/AIRWALLEX_SPEND_API_KEY (falls back to main key if unset). Probe kept at mcp-servers/airwallex/test-spend-access.js (prefers spend key when set; no secret literals). PENDING JOHN: dashboard Settings > Developer > API keys > create Scoped API key with org-level Spend Read+Write, then add the two env vars to .mcp.json krave-airwallex env and re-run the probe (expect 400 "field required" = in). File-upload attachments may also need the spend token — verify on first live bill with attachment.

[2026-06-11] DECISION: Airwallex Spend API access LIVE — via new org-scoped key `spendcreatekey` (client ID rPx10CsAT8OxsqRcNw6Cdw) in .mcp.json AIRWALLEX_SPEND_* env vars | REASONING: After John created the scoped key, login still returned edge 403 HTML. Dashboard inspection (playwright-cdp on John's logged-in Chrome — bridge worked this time despite earlier hang reports) showed the pasted AIRWALLEX_SPEND_CLIENT_ID (23 chars) matched NONE of the four scoped keys (all 22 chars) — a mis-paste, not an IP block (IP whitelist column = "-" for all keys). Corrected the client ID to spendcreatekey's via surgical regex on .mcp.json (secret untouched). Probe now: login OK; POST /spend/bills/create empty body -> 400 Malformed request (access granted); mark_as_paid fake id -> 404; GET /spend/vendors -> 200. | CONTEXT: Discovered working values: legal_entity_id = le_Zxw2-ECjOaKKebIGraD1AA (constant across all 20 visible bills); 17 vendors listed with UUIDs (creators incl. A.G. Mapula, B. Perez, J.M. Domingo). x-on-behalf-of irrelevant for spend key (works with and without). NOTE: the running krave-airwallex MCP instance loaded env at session start — restart Claude Code (or reconnect MCP) before the airwallex_* spend tools pick up the new key. Creator-invoice flow REMAINS forward-by-email; switch decision pending John (docs updated: SKILL.md x2 + SOP). Next: first live test bill (draft, low-stakes, with PDF attachment to also verify cross-key file_id acceptance) after John's go-ahead.

[2026-06-11] FINDING: First live API-created bill succeeded — but lands AWAITING_PAYMENT (skips approval), and file attachments are blocked for BOTH keys | REASONING: Test bill 877ddc5f-3d7c-32d5-aba6-4e2e7e1ba703 (Stashworks, 1.00 SGD, TEST-API-DO-NOT-PAY, John-confirmed params) created via spend key -> 201, verified via GET. (1) STATUS GOTCHA: it went straight to AWAITING_PAYMENT with approvers=[] — API-created bills bypass the human review that email-forwarded drafts get from John. Before any flow switch, either configure Spend approval rules in the dashboard or accept that API bills are immediately payable. (2) ATTACHMENTS BLOCKED: POST /api/v1/files/upload -> 401 Access Denied with main key, 401 "Admin permissions required" with spend scoped key — so invoice PDFs cannot currently be attached to API bills with either key; airwallex_upload_file MCP tool is broken for this path. Open question for Bing/Airwallex: which endpoint+permission covers bill attachments for scoped keys. | CONTEXT: Test bill left in place for John to inspect then DELETE in dashboard (Spend > Bills > TEST-API-DO-NOT-PAY). test-create-bill.mjs deleted after run; reusable access probe remains at mcp-servers/airwallex/test-spend-access.js. MCP airwallex_create_bill code path now fully validated live except attachments.

[2026-06-11] FINDING: File upload UNBLOCKED (201 with file_id) after adding Upload Files (Write) to spendcreatekey — but attaching to bills still fails; attachment-ID provenance is an API gap | REASONING: Airwallex support: upload needs account-level Supporting Services > Upload Files (Write) scope (w:awx_action:file_upload) on the scoped key, host files.airwallex.com not api.airwallex.com. Permission added via John's Chrome (playwright-cdp; John completed SMS 2FA), verified 201 upload. However /spend/bills/create rejects the returned file_id ("Malformed request body" — schema wants array[string(uuid)]) and 500s on the UUID embedded inside the file_id (2ab2d5a8-... not a known attachment entity). The docs never say where bill attachment IDs come from — likely a missing "create attachment" step or files-service/Spend disconnect. Question sent to Bing Quek (release feedback). | CONTEXT: index.js airwallexUploadFile now uses FILES_BASE_URL=files.airwallex.com + spend-key token; probe includes an upload check. Attachment wiring in airwallex_create_bill left passing the raw ID through — revisit when Airwallex answers. Two test bills to delete in dashboard: TEST-API-DO-NOT-PAY (877ddc5f, confirmed) and possibly an orphan from the 500 attempt (TEST-API-ATTACHMENT-DO-NOT-PAY — check Bills page). NOTE: GET /spend/bills list does NOT show the 06-11 API-created test bill in its newest-first results (top entry 06-09) — list sort/filter behavior unverified; rely on GET by id.

[2026-06-12] DECISION: Creator-invoice flow — Phase 1 of API migration shipped (manual /invoice-triage path now creates Airwallex bills via API; n8n + bot stay email-forward until Phase 2) | REASONING: John approved full automation: API-create bills with NO attachment + per-bill #ops-command flag so he uploads the PDF manually (Airwallex confirmed via Thibault Genaitay 2026-06-12 that bill attachments are unsupported until ~Aug 2026 Q3, and they removed the attachments field from spend/bills/create). Locked rules: vendor = invoice payee not sender; hardcoded vendor map (Butanas 1c584a1f→PHP, JM Domingo 6a951c12→USD, Baste/Sebastian Perez a0a99771→SGD, Marian Borynets 287b6774→invoice ccy) + live list_vendors; unmatched vendor → create PROFILE ONLY (name+country, never bank details — PDF stays payment source of truth) + 🚨 NEW VENDOR; invoice# missing → INV-+7 random digits; currency missing → infer from bank country; convert at live rate ×0.97 when invoice ccy ≠ payout ccy (note in description + 🚨 CONVERTED); reply ONCE after all bills staged (bounce immediately, known-sender gated per 2026-06-12 incident standard); post-create guard via get_bill; dedup = tracker external_id + live vendor+invoice# + request_id. Reporting design (John delegated): per-bill 🧾 upload-PDF notice to #ops-command C0AQZGJDR38, run summary, and 🚨 loud flags only where money can go wrong silently (new vendor / conversion / amount-mismatch / guard-mismatch). | CONTEXT: MCP index.js — removed attachment_file_id from create_bill (would 400 now), updated upload_file/create_bill descriptions, body no longer sends attachments. Synced all 5 files: .claude + .agents SKILL.md (full API flow), SOP (Overview/Step2/Step3/Step6/Currency/Exceptions/Reporting + vendor map), WORKFLOWS.md + README + deploy-script header (migration notes; deploy docstring corrected — it falsely claimed API creation). NOT YET DONE: live validation run on a real pending invoice; Phase 2 (krave-bot + n8n DbIJYYQ3FE4HKprB via httpCustomAuth Spend credential). FX rate source = live Airwallex rate (endpoint to confirm at executor build). Vendor create_vendor not yet live-tested (address fields deferred pending create schema). legal_entity_id le_Zxw2-ECjOaKKebIGraD1AA.

[2026-06-15] DECISION: Inbox Triage + Morning Coffee refactor — triage becomes a silent sorter (inbox = actionable queue), Morning Coffee becomes Noa's single email surface (now incl. FYI), and its ClickUp + Slack sources were repaired | REASONING: John wanted the labels to auto-sort and "flag everything to Noa in Morning Coffee that needs her attention or is just FYI." Two real defects surfaced when verifying live: (1) Morning Coffee's ClickUp "Project Pulse" had been empty for ~a week — it read a `clickup_emails` var that the label-based Step 2 never populates (ClickUp notifs are `EA/Auto-Sorted` + archived, never in the Urgent/Needs-Reply buckets); confirmed 4 real status emails sat in Gmail unseen. (2) Slack only ever showed the two bot-token channels (Step 4); the `slack-noa` scan (Step 5) was dead because it excluded `is_dormant` channels (Slack flags ~all her active channels dormant) and used a stale priority list that missed every `krave-x-*`/`int-*`/reporting channel. slack-noa itself works (she's in ~40 channels). | CONTEXT: Inbox Triage v2 (`EuT6REDs5PUaoycE`) deploy script — (a) over-archiving fix: new `archive_ok` flag in "Restore Email Metadata" archives ONLY `EA/FYI` + `EA/Auto-Sorted`; Urgent/Needs-Reply/Unsure + `_Payment_Received` stay in inbox (ends "inbox zero", per John option 1); (b) mis-tiering fix: KNOWN-contact match now uses sender-only `senderHay` (name+email), not the body haystack, killing false Needs-Reply from short tokens (amy/shin/cody) hitting body text. Morning Coffee (.claude + .agents): added `EA/FYI` bucket + a compact "Just So You Know (FYI)" section (one-liners, capped 5, below attention); Project Pulse now uses a direct `from:notifications@tasks.clickup.com newer_than:1d` query; Slack Step 5 now scans all `is_member && !is_archived` channels (dropped dormant filter + stale list), ranked by recency, cap 20, 7 bullets. Triage `#ops-command` summary kept as John's audit view (no Noa DM — corrected stale "DM Noa" claims in WORKFLOWS.md + README.md, both described a Noa DM the live v2 never sends). Test `inbox-triage-daily.test.js` was orphaned/red (asserted a v1 design that never shipped) — rewritten to assert the live v2 + new behavior; passes. Discovered dangling ref: inbox-triage SKILL.md pointed to nonexistent `references/sops/inbox-triage.md` → repointed to WORKFLOWS.md Workflow 5. PENDING: redeploy the triage deploy script to live n8n + verify (archive_ok wiring + sender-match). Synced: deploy-inbox-triage-daily.js, inbox-triage SKILL.md x2, morning-coffee SKILL.md x2, WORKFLOWS.md (W5 purpose/outputs/index), README.md (W5 section), inbox-triage-daily.test.js. See [[reference_gmail_airwallex_filters]].

[2026-06-12] BUILD: Phase 2a — krave-bot wired for Airwallex Spend bill creation (built + syntax-checked, NOT deployed) | REASONING: Promoting the validated Phase-1 API flow into the Slack bot. projects/krave-bot/tools/airwallex.js: dual-key auth (spend key for /api/v1/spend/*, main key else, per-key token caches), fixed legacy /api/v1/bills → /api/v1/spend/bills on list/get, added listVendors/createVendor(profile-only, no bank details)/createBill(no attachment field)/markBillPaid/fxRate, added /api/v1/fx/ to versioned paths. FX rate VERIFIED live: GET /api/v1/fx/rates/current?buy_currency=PHP&sell_currency=USD with MAIN key + x-api-version 2024-09-27 (or 2025-06-16) → USDPHP 60.10; spend key 401s on FX (correct — FX is main-key). system-prompt.js CREATOR/AP section rewritten to the API flow (classify→parse→validate→resolve vendor→fx convert ×0.97→create_bill→get_bill guard→#ops-command 🧾 flag→reply once→tracker) + per-vendor payout currency (Butanas PHP, Domingo USD, Baste SGD, Marian invoice ccy) + INV-7digit. | CONTEXT: GO-LIVE PENDING (autonomous financial action — needs approval): (1) set Render env AIRWALLEX_SPEND_CLIENT_ID + AIRWALLEX_SPEND_API_KEY on service krave-ai (srv-d87b7728qa3s73d53njg) — without them spend calls fall back to main key and 401; (2) git push github master (auto-deploys bot). Until deployed, the LIVE bot still forwards-by-email, so skill/SOP keep the "bot = email forward until Phase 2" label (flip at deploy). NOT STARTED: Phase 2b = n8n email workflow DbIJYYQ3FE4HKprB rebuild (httpCustomAuth Spend credential + vendor/FX/create/guard/flag nodes) — larger, higher-risk, deserves its own focused pass. Note: bot system-prompt references subscription tools (create/get/list_subscriptions) that are absent from tools/airwallex.js — pre-existing gap, unrelated to this change.

[2026-06-15] TEST + FIX: krave-bot Phase 2a supervised first run — 4 issues found; 3 fixed in prompt, 1 (draft/upload) root-caused to approval routing | REASONING: John DM'd the bot a real Stashworks invoice (INV-007838, SGD 1,632.30). Bot created bill d58900dc successfully BUT: (1) the requester thread reply included the Airwallex bill link + bill ID — only John has Airwallex access, strategists must not see it; (2) bill landed finalized/AWAITING_PAYMENT so John could NOT upload the PDF in the webapp (needs a reviewable state); (3) due date mis-parsed ("17 Jun" with no clear source); (4) tracker append failed on the sheet tab name. John deleted d58900dc; will re-run after fixes. | CONTEXT: FIXES SHIPPED (prompt + skill + SOP synced): reply is now plain confirmation only, never the Airwallex link/ID (that goes only to the #ops-command 🧾 flag); due date must use the invoice's printed due date/terms exactly, never invent, Friday-of-week only if truly absent, and show the source in the flag; tracker tab name flagged as EM-DASH "Krave — Creator & AP Bills Tracker" (the bug — bot passed a hyphen; tool also defaults to the only tab if omitted). DRAFT/UPLOAD ROOT CAUSE: get_bill on the 06-09 Marian bill (2ca3c0a3) shows email-forward bills land AWAITING_APPROVAL with the PDF auto-attached + approver noa@kravemedia.co + external_id null; API bills set external_id, have NO approver/PDF, and skip to AWAITING_PAYMENT (unuploadable). create_bill API has no status/approver field — routing to approval is Airwallex-side (Spend approval policy). Likely API integrations bypass approval by design. NEEDS: John check Settings→Spend approvals, and/or ask Thibault how to route API bills through approval (AWAITING_APPROVAL) or create as DRAFT. Until resolved, API bills are not PDF-uploadable — DO NOT re-run for production until fixed. BACKFILL DONE: 3 most-recent bills missing from tracker added (rows 17-19): Kang Ying Xuan IVN-2026-03 180 SGD 8d9028e1, Marian Borynets "Invoice for May" 1300 USD 2ca3c0a3, Hailey Nolin #47 450 USD 92bb561a — all AWAITING_APPROVAL, created 06-09.

[2026-06-15] DECISION: Creator-invoice flow restructured to PREP & HANDOFF; n8n email workflow DEACTIVATED for a safety bug; hardcoded sender allowlist enforced | REASONING: John clarified after the supervised test: (1) only DRAFT bills allow PDF upload — AWAITING_APPROVAL is already finalized; (2) the API can't create DRAFT or attach PDFs until ~Aug 2026, and API bills land AWAITING_PAYMENT (unuploadable); (3) email-forward is broken (not forwarding). So neither automated path produces what's needed (draft + PDF). New model: automation parses/validates/does FX, posts a ready-to-create PREP PACKAGE to #ops-command, replies to the team (feels automated), logs the tracker — John creates the DRAFT bill manually (vendor → fields → upload PDF → submit). No airwallex_create_bill in the flow (kept for the Aug 2026 auto-create flip). | CONTEXT: SAFETY: verified the n8n email workflow's known-sender gate covered ONLY the missing-bank-details path; the success reply (Reply Fallback n27) was UNGATED and fired even on forward failure (continueOnFail) — exactly the 2026-06-12 client-as-creator exposure, still live. DEACTIVATED DbIJYYQ3FE4HKprB (POST /deactivate, active=false) pending prep-and-handoff rebuild. ALLOWLIST: replies now go ONLY to hardcoded Krave team (John, Noa, Jeneena, Amanda, Shin, Sybil @kravemedia.co); any other sender → no reply, #ops-command 🚨 UNKNOWN SENDER flag. bot tools/airwallex.js keeps create_bill/create_vendor/fx_rate for August but the prompt no longer calls create. Synced all 6 files: bot system-prompt.js, .claude + .agents SKILL.md, SOP, WORKFLOWS.md row 22, README, deploy-script header. Also fixed 3 supervised-test bugs earlier this session (reply leaked Airwallex link; due-date parse; tracker tab em-dash) and backfilled 3 missing bills (rows 17-19). Aug 2026 = flip back to auto-create (DRAFT + API attachments land).

[2026-06-15] BUILD + FIX: n8n email path rebuilt to prep-and-handoff (deployed INACTIVE); krave-bot tracker-logging bug fixed (wrong hardcoded spreadsheet) | REASONING: (A) EMAIL REBUILD — rebuilt DbIJYYQ3FE4HKprB from forward-by-email to prep-and-handoff: removed the forward node + ungated success reply; added Build Prep Context (vendor exists/NEW + payout ccy from a hardcoded map, #ops-command package text, senderKnown via hardcoded @kravemedia.co domain, pairedItem set so refs survive the Slack HTTP node), a Known Sender? (reply gate) IF on the success path (one-line "Received — staged for payment" to allowlisted senders only; others no reply), and Log Prepped to Bills Tab (status "Prepped — awaiting manual creation", blank Bill ID — logs at PREP time, before John acts). Deploy activation now GATED behind ACTIVATE=1 (deploys inactive by default; this workflow sends email + touches money). Live FX deferred in email v1 (package flags payout-currency conversion for John to apply at creation). Deployed INACTIVE, verified active=false + new nodes present + forward node gone (30 nodes). (B) TRACKER BUG — John: the bot didn't fill the tracker. Root cause: projects/krave-bot/tools/sheets.js hardcoded SHEET_ID=1u5Ink… (the SUPERSEDED AR sheet) and accepted only a tab name, no spreadsheet ID — so AP-bill logging to the Creator & AP Bills Tracker (14kiX9…) never landed. Fix: added spreadsheet_id param (alias "bills" → 14kiX9…) to getRows/appendRow/updateRow + definitions; AR default unchanged (backward-compatible). system-prompt step 8 now MANDATORY + passes spreadsheet_id "bills" + the em-dash tab; logs at prep time even before John creates the bill. | CONTEXT: Synced WORKFLOWS.md (row + Workflow 22 full section: node flow, key logic, outputs, error handling), README row, SOP, .claude + .agents skills. n8n email tracker logging uses BILLS_SHEET_ID (correct); only the bot tool was mis-pointed. NEXT: webhook-test the email path then ACTIVATE=1; John re-tests bot to confirm tracker fills.

[2026-06-15] DECISION: Tracker model simplified to "EOD reconcile mirrors Airwallex" (John's call — drop status/prep-writes) | REASONING: John: "we dont need status, we just need the list — schedule every EOD to list Airwallex bills, cross-check the sheet, fill what's not there." Built n8n-workflows/reconcile-creator-bills.mjs (dry-run default; --apply to write): logs in via spend key, GET /spend/bills + /spend/vendors (paginated), reads the Creator & AP Bills Tracker, then FILLS blank Bill ID cells (match by normalized invoice# + amount + currency) and APPENDS bills with no matching row. No status lifecycle — Bill ID present = exists in Airwallex. First --apply run: filled 10 existing rows' Bill IDs + appended 7 bills (incl. Stashworks INV-007838 81a9074d which John recreated manually, Weng Shu Yi KM106, Amanda Ng INV-02062026, etc.; one "(unknown vendor)" = Baste 518202-BPerez whose vendor_id wasn't in the vendors page). | CONTEXT: This makes EOD reconcile the SOLE tracker populator. PENDING: (1) remove the prep-time tracker write from bot system-prompt + n8n Log Prepped node (else currency-converted bills like Butanas USD→PHP won't match the prep row and duplicate); (2) schedule the reconcile EOD (home TBD — local Task Scheduler vs server-side Render cron/n8n; script needs spend key + Google SA, both available locally; SA is a FILE locally / would be JSON env server-side). Script reads .env + .mcp.json + GOOGLE_SERVICE_ACCOUNT_KEY_FILE.

[2026-06-15] DECISION: EOD bills reconcile shipped server-side + verified; prep-time tracker writes removed (bot + n8n) | REASONING: John's model — tracker = mirror of Airwallex bills, filled by an EOD job; no status, no prep-time rows. Built: (a) projects/krave-bot/tools/reconcile.js + server.js POST /cron/reconcile-bills (x-cron-secret gated) — lists Airwallex Spend bills + vendors, reads tracker, fills blank Bill IDs by invoice#+amount+currency, appends missing; reuses the bot's spend key + Google SA (no secret replication). (b) n8n trigger workflow FdtmNRozitg711BQ "Krave — Creator Bills EOD Reconcile" (active, 19:00 PHT Mon–Fri) POSTs the endpoint. (c) CRON_SECRET generated → set on krave-ai Render env + appended to .env (deploy script bakes it into the n8n HTTP header). REMOVED prep-time tracker writes: bot system-prompt step (no sheets write) + n8n email "Log Prepped to Bills Tab" node (rewired reply→mark read) — a prep-time row would duplicate/mismatch on currency-converted bills (Butanas USD→PHP). | CONTEXT: VERIFIED end-to-end: curl POST /cron/reconcile-bills with secret → 200 {total:20, filled:0, added:0} (idempotent, sheet already synced from the earlier --apply run that filled 10 + added 7). Standalone util n8n-workflows/reconcile-creator-bills.mjs (dry-run/--apply) kept for manual runs. Synced WORKFLOWS.md (Workflow 25 section + index row 25 + Workflow 22 node-flow/outputs), README row, SOP (Step 5 rewritten, banner, Step 3.4), .claude + .agents skills. Email path still INACTIVE (ACTIVATE=1 after webhook test). bot sheets.js "bills" alias retained (unused by reconcile but harmless). Pagination note: reconcile page_size=100 covers current ~20 bills; add cursor pagination if >100.

[2026-06-15] DECISION: Email path (DbIJYYQ3FE4HKprB) tested end-to-end + ACTIVATED — full creator-invoice automation now live on all channels | REASONING: Controlled test (John-approved): sent a sample creator invoice noa→john twice. Run 1 (PDF marked "PIPELINE TEST/IGNORE") → Claude correctly classified is_invoice=FALSE → not-invoice path posted an #ops-command skip notice + marked read (incident guard working). Run 2 (clean "Jordan Avery" invoice, no test markers, from internal forwarder noa@) → is_invoice=TRUE → SUCCESS path fully exercised: Build Prep Context → #ops-command prep package ("Ready to create — Jordan Avery", NEW VENDOR flag, invoice/amount/bank/PDF pointer) → Known Sender gate (noa@ allowlisted) → one-line reply "Received — staged for payment" → marked read → NO tracker write. Confirmed the pairedItem fix resolves $('Build Prep Context').item through the Slack HTTP node + IF gate, AND that a legitimate internal-forwarded invoice classifies correctly (guard doesn't false-reject strategist forwards). Activated via POST /api/v1/workflows/{id}/activate → active=true. | CONTEXT: Earlier ACTIVATE=1 was blocked by the permission classifier (wanted test-before-activate + specific authorization) — correct guardrail; activated after the passing test + John's explicit "controlled test then activate" choice. Activation stays gated behind ACTIVATE=1 in the deploy script (plain redeploy won't re-enable). Test artifacts (harmless, John/Noa can ignore): a "Jordan Avery" prep package in #ops-command, a "Received — staged for payment" reply to noa@, two test emails marked read in john@ inbox; NO bill created, NO tracker row. Local test files (test-invoice.pdf, make-test-invoice.mjs) deleted. Docs flipped INACTIVE→ACTIVE across WORKFLOWS (row 22 + Workflow 22 purpose), README, SOP, both skills, deploy header. FULL SYSTEM NOW LIVE: Slack bot + email (prep-and-handoff, hardcoded allowlist, one-line reply) + EOD reconcile (#25, mirrors Airwallex bills into tracker). Aug 2026 = flip to auto-create.

[2026-06-15] FOLLOW-UP: Halo-Home Sync (Jun 12) action items dispatched — IG chatbot + creator-outreach builds gated on info-gathering before any code | REASONING: Analyzed the sync transcript. John's owned actions: the IG DM chatbot (Meta-side prerequisites), creator-outreach automation (needs Alleah's current process), and the Halo intel report organic+paid upgrade (that one already shipped on today's Monday run, so out of scope this round). Both new builds are blocked on inputs only the team can supply, so requirements-gathering precedes any build. | CONTEXT: Posted via slack-john (John AI) to #krave-homewithhalo (C0A22NPLV38): (1) Shin — combined msg: IG chatbot Meta prerequisites (admin/developer access to Halo Meta Business, IG Professional + FB Page link, Business Verification, Meta app approval; flagged the multi-day messaging-permission review as the critical path) + request for ad-concept formats/definitions/sample structures for the report's paid section; (2) Alleah — write-up request for her creator-outreach process (sources, flow, templates, tracking, volume, pain points). Summary posted to #ops-command (C0AQZGJDR38). PENDING: Shin grants Meta access + confirms which email to add + sends ad formats; Alleah sends outreach write-up. No code or workflow changes this round. See [[project_halo_home_shopify]].

[2026-06-16] FIX: Misrouted creator-bill row removed from the CLIENT/AR tracker + sheets.js hardened against silent cross-tracker writes | REASONING: John reported that yesterday's (06-15) creator-invoice test wrote into the CLIENT/AR tracker (1u5Ink…) instead of the Creator & AP Bills Tracker (14kiX9…). Root cause: projects/krave-bot/tools/sheets.js resolveSheetId() SILENTLY DEFAULTS to the AR sheet when spreadsheet_id is omitted; a pre-fix bot build (before the 06-15 "stop prep-time tracker writes" commits 9ee3a1b/03d7ff9) appended a Stashworks prep row WITHOUT spreadsheet_id:"bills", so it landed in the AR sheet in BILLS column order (mismatched against AR columns). Confirmed: the offending row was the last AR row (Stashworks INV-007838, 1632.3 SGD, status "Prepped — awaiting manual creation"); the SAME bill is correctly in the bills tracker with real Bill ID 81a9074d (added by EOD reconcile) — so the AR row was a pure duplicate-in-wrong-place, no data lost. | CONTEXT: (1) Deleted the AR row via n8n-workflows/patch-remove-misrouted-stashworks-row.mjs (fail-safe: matches only vendor+invoice+status, aborts on ≠1 match; dry-run then --apply → deleted AR row 41). (2) Hardened sheets.js: resolveSheetId(spreadsheet_id, sheet) now THROWS if a write targets the bills tab ("Krave — Creator & AP Bills Tracker") without routing to the bills spreadsheet — fail-fast before token fetch; all 3 handlers (get/append/update) updated; AR-default + reconcile "bills" paths unchanged. Verified with an offline guard test (blocks bills-tab-w/o-routing, allows AR default + routed bills). The n8n email workflow was never vulnerable (its Sheets node hardcodes BILLS_SHEET_ID); only the bot tool had the footgun. NOT DEPLOYED: fix takes effect on next `git push github master` (auto-deploys krave-ai Render service) — pending John's go-ahead. The rename of the bills spreadsheet's FILE title is harmless (code keys off ID + tab name, both unchanged).
